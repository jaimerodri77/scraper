name: Tennis Scraper Histórico

on:
  workflow_dispatch:
    inputs:
      fecha:
        description: 'Fecha específica para prueba (YYYY-MM-DD). Deja vacío para correr histórico completo.'
        required: false
        default: ''

jobs:
  scrape-historico:
    runs-on: ubuntu-latest
    timeout-minutes: 340 

    permissions:
      contents: write

    steps:
      - name: Checkout repositorio
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          pip install pandas playwright
          python -m playwright install chromium
          python -m playwright install-deps chromium

      - name: Preparar carpeta de datos
        run: mkdir -p datos

      - name: Ejecutar scraper
        run: |
          if [ -n "${{ github.event.inputs.fecha }}" ]; then
            echo "🚀 MODO PRUEBA: Fecha ${{ github.event.inputs.fecha }}"
            python scraper_historico.py --fecha ${{ github.event.inputs.fecha }}
          else
            echo "📅 MODO HISTÓRICO COMPLETO"
            python scraper_historico.py
          fi

      - name: Guardar CSV en repositorio
        if: always()
        run: |
          git config --global user.email "scraperbot@github.com"
          git config --global user.name "TennisScraperBot"
          git add datos/*.csv || true
          if git diff --staged --quiet; then
            echo "No hay cambios en el CSV."
          else
            git commit -m "Update tenis csv: $(date +'%Y-%m-%d %H:%M')"
            git push
          fi


